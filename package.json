{
  "name": "tapsa-combined",
  "version": "2.0.0",
  "description": "Combined TAPSA Bulk SMS web application (frontend + backend)",
  "main": "backend/index.js",
  "scripts": {
    "install:all": "npm install && cd backend && npm install",
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev",
    "build": "echo 'No build step required for plain HTML/CSS/JS frontend'",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "keywords": [
    "sms",
    "bulksms",
    "tanzania",
    "africastalking",
    "firebase",
    "fullstack"
  ],
  "author": "Lazaro Mtaju",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  }
}
